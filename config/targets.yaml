# VulnoraIQ Target Configuration
# --------------------------------
# Define your own authorised AI application targets here.
# Scans require at least one target with a real endpoint.
# Test fixture targets are not allowed in normal runtime;
# set VULNORAIQ_ALLOW_TEST_FIXTURE_TARGETS=true to enable them.

targets: {}
  # Example: OpenAI-compatible chat completions endpoint
  # my_llm_endpoint:
  #   name: My LLM Endpoint
  #   type: chat_completions
  #   base_url: http://127.0.0.1:8080
  #   endpoint_path: /v1/chat/completions
  #   model: my-model
  #   auth_token_env: LLM_VAPT_TARGET_TOKEN
  #   timeout: 30
  #   authorisation_required: true
  #   safety_profile: local_lab_safe
  #   environment: local
  #   owner: {name: "", contact: "team@example.com"}
